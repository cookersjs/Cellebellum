// An environment variable config file for the front-end

(function (window) {
    'use strict';
    window.__env = window.__env || {};

    // API url
    window.__env.apiUrl = 'http://10.30.64.135:3001/api';

    // Base url
    window.__env.baseUrl = '/';
})(window);
